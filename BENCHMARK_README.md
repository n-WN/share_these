# 带宽测试基准脚本集合

## 概述

本分支包含经过修正的 HTTP 服务器带宽测试脚本，解决了本地回环接口重复计算带宽的问题。

## 核心修复

**问题**: 之前的带宽计算将 TX 和 RX 相加，导致本地测试时带宽被重复计算（例如：显示 85.87 MB/s 总带宽，但实际个别速率为 ↑666.81 ↓666.81 MB/s）。

**解决方案**: 对于本地回环接口，采用 `max(TX, RX)` 而非 `TX + RX`，避免计算同一数据流的两次测量。

## 测试脚本

### 1. `monitor/concurrent_50_test.sh`
- **服务器**: Bash 内置 HTTP 服务器
- **特点**: 修正本地回环重复计算
- **用途**: 基础并发性能测试

### 2. `monitor/concurrent_50_test_http_file_server.sh`  
- **服务器**: 专用 HTTP 文件服务器
- **特点**: 高性能文件传输
- **用途**: 专业文件服务器性能测试

### 3. `monitor/concurrent_50_test_python_server.sh`
- **服务器**: Python HTTP 服务器
- **特点**: 修正本地回环重复计算，单线程限制分析
- **用途**: Python 服务器性能基线测试

### 4. `monitor/server_comparison_test.sh`
- **功能**: 运行所有三个测试并生成对比报告
- **输出**: CSV 格式的性能对比数据
- **用途**: 自动化服务器性能对比

## 使用方法

```bash
# 运行单个测试
cd monitor
./concurrent_50_test.sh

# 运行完整对比测试
./server_comparison_test.sh
```

## 技术细节

- **带宽计算修正**: `max(TX, RX)` 替代 `TX + RX`
- **平滑算法**: 简化为 3 样本平均值
- **显示格式**: 显示修正前后的带宽值对比
- **错误处理**: 改进的服务器启动和测试文件验证
